query BivariateStatisticAxis($polygon: String!) {
  polygonStatistic(polygonStatisticRequest: { polygon: $polygon }) {
    bivariateStatistic {
      axis {
        label
        steps {
          label
          value
        }
        quality
        quotient
      }
    }
  }
}
