query UrbanCoreAnalytics($polygon: JSON!) {
  polygonStatistic(polygonStatisticRequest: { polygon: $polygon }) {
    analytics {
      urbanCore {
        urbanCorePopulation
        urbanCoreAreaKm2
        totalPopulatedAreaKm2
      }
    }
  }
}
